apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mateusclira-ingress-tls
spec:
  tls:
  - hosts:
      - https-example.foo.com
    secretName: mateusclira-tls
  rules:
  - host: https-example.localhost
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: service1
            port:
              number: 80
